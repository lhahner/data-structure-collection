#include <stdlib.h>
#include <stdio.h>

struct Node{
	int key;
	struct Node *left;
	struct Node *right;
};

Node addNode(struct Node *obj, int _key){
	obj = malloc(sizeof(Node));
	if(obj == NULL){
		obj.key = _key;
		obj.left = NULL;
		obj.right = NULL;
	}
	else {
	 	if(obj.key < _key){
			tree.left = 
		}		
	}
}



void main(){
	
}
